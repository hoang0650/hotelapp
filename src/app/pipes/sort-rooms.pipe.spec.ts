import { SortRoomsPipe } from './sort-rooms.pipe';

describe('SortRoomsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRoomsPipe();
    expect(pipe).toBeTruthy();
  });
});
